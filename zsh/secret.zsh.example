# Example secrets file. Copy to secret.zsh (gitignored) and add private exports.
# Most credentials should live in fnox (encrypted with age) instead — see
# bin/bootstrap-secrets and ~/.config/fnox/config.toml. Use this file only
# for one-off exports that don't fit fnox.
# export OPENAI_API_KEY="..."
